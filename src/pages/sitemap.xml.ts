import projects from '../data/projects.json';import posts from '../data/writing.json';
export function GET(){const urls=['','about/','playground/','writing/',...projects.map(p=>`work/${p.slug}/`),...posts.map(p=>`writing/${p.slug}/`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>https://jaydenjoony05.github.io/${u}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml'}});}
